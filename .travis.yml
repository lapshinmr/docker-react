language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t lapshinmr/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true lapshinmr/docker-react npm run test
